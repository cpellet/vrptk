#ifndef VRPTK_TEST_H
#define VRPTK_TEST_H

// #include <torch/torch.h>

int test() {
    // torch::Tensor tensor = torch::rand({2, 3});
    // std::cout << tensor << std::endl;
    return 0;
}

#endif // VRPTK_TEST_H